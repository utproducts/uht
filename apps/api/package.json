{
  "name": "@uht/api",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply uht-db",
    "db:migrate:local": "wrangler d1 migrations apply uht-db --local"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "@hono/zod-validator": "^0.4.0",
    "zod": "^3.23.0",
    "jose": "^5.9.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241018.0",
    "wrangler": "^3.86.0",
    "typescript": "^5.6.0"
  }
}
